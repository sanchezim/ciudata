<?php

use Illuminate\Support\Facades\Route;

// Route::get('lang/{lang}', function ($lang) {
//     if (!in_array($lang, ['en', 'es', 'fr'])) {
//         abort(400);
//     }
//     session(['lang' => $lang]);
//     dd(session()->all());
// });
